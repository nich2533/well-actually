# src/

Your application code lives here. This starter ships empty on purpose — the point of `well-actually` is the *context* layout around your code (`CLAUDE.md`, `rules/`, `documentation/`), not the code itself.

Drop your project in, then make the `rules/` and `documentation/` files describe it.
